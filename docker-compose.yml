---
services:
  backend:
    image: hiteshpatil989/employee-tracker-backend:latest
    container_name: produx-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      MONGODB_URI: >
        mongodb+srv://sunny:Sunnyp123@cluster0.key5wpm.mongodb.net/
        produx?retryWrites=true&w=majority&appName=Cluster0
      JWT_SECRET: "mysecret123"
      JWT_EXPIRES_IN: "7d"
      NODE_ENV: "production"
      FRONTEND_URL: "http://18.209.47.148"
      PORT: "5000"

  frontend:
    image: hiteshpatil989/employee-tracker-frontend:latest
    container_name: produx-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend